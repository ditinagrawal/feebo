import { Login } from "@/components/auth/login";

export const metadata = {
  title: "Sign in to Feebo",
  description: "Please sign in to continue",
};

const LoginPage = () => {
  return <Login />;
};

export default LoginPage;
